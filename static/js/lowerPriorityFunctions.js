// Functions that can run at 30 frames a second instead of 60.
import {ctx, canvas} from './canvas.js';

export async function lowerPriorityLoop() {
	// Health draw.
	// Unimplemented. We actually likely won't do this.
}