let config = {};

config.cert = ""; // The location of the https certificate. Leave blank if not applicable.
config.key = ""; // The location of the https certificate. Leave key if not applicable.

module.exports.config = config;